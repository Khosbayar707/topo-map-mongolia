name = "mongolia-ortho"
compatibility_date = "2024-01-01"
pages_build_output_dir = "deploy"

# Топо сайттай ижил bucket — одоо байгаа ортофотонууд хэвээр ажиллана
[[r2_buckets]]
binding = "BUCKET"
bucket_name = "topo-map-tiles"
